from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='API Simple que sirve un modelo para la prevención del Churn',
    author='Giocrisrai',
    license='MIT',
)
